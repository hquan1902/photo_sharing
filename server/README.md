# Photo Sharing API Server

Backend API for Problem 2.

## Endpoints

- `GET /test/info`
- `GET /user/list`
- `GET /user/:id`
- `GET /photosOfUser/:id`

## Run

1. Install dependencies in `server/`
2. Start server on port `8080`

This server is designed to work with frontend `fetchModel` calls.
